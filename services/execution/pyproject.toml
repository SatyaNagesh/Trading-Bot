[tool.poetry]
name = "execution-engine"
version = "0.1.0"
description = "QuantLab AI Execution Engine"
packages = [{ include = "main.py" }, { include = "service.py" }]

[tool.poetry.dependencies]
python = "^3.13"
fastapi = "^0.115"
