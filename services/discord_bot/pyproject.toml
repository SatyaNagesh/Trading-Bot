[tool.poetry]
name = "quantlab-discord-bot"
version = "0.1.0"
description = "QuantLab AI — Discord Bot (API Client)"
authors = ["Satya Nagesh"]

[tool.poetry.dependencies]
python = "^3.13"
discord-py = "^2.4"
httpx = "^0.27"
pydantic = "^2.0"
python-dotenv = "^1.0"
structlog = "^24.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
