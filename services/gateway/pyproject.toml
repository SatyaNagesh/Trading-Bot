[tool.poetry]
name = "auth-gateway"
version = "0.1.0"
description = "QuantLab AI Auth & API Gateway"
packages = [{ include = "main.py" }]

[tool.poetry.dependencies]
python = "^3.13"
fastapi = "^0.115"
pyjwt = "^2.8"
